<Controls:MetroWindow x:Class="GormWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:GormWpf"
        mc:Ignorable="d"
        Title="Gorm" Height="350" Width="525" Loaded="MetroWindow_Loaded">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=StatusUpdates}" Margin="10,0,105,10"/>

        <Button x:Name="StartButton" Command="{Binding StartCommand}" Content="Start" HorizontalAlignment="Left" Margin="433,10,0,0" VerticalAlignment="Top" Width="74"/>
        <!--<Button x:Name="StartClientButton" Command="{Binding StartClientCommand}" Content="Client Start" HorizontalAlignment="Left" Margin="417,40,0,0" VerticalAlignment="Top" Width="74"/>-->
    </Grid>
</Controls:MetroWindow>
